# Base URL of the Staccato backend public API (server-side fetches).
BACKEND_URL=http://localhost:8000

# Same URL, exposed to the browser for any client-side fetches.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
